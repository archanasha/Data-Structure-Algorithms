//#1 - Sort 10 schools around your house by distance:
insertion sort - very small array and it has space complexity O(n)

//#2 - eBay sorts listings by the current Bid amount:
radix or counting sort - it includes numbers and number cannot bigger

//#3 - Sort scores on ESPN
Quick sort - space complexity O(log(n))

//#4 - Massive database (can't fit all into memory) needs to sort through past year's user data
Merge Sort - worst case time complexity of O(n log(n))

//#5 - Almost sorted Udemy review data needs to update and add 2 new reviews
Insertion Sort - almost sorted,

    //#6 - Temperature Records for the past 50 years in Canada
    radix or counting Sort - if no decimal places
Quick sort if decimal places

//#7 - Large user name database needs to be sorted. Data is very random.
Quick sort - pivot picking is easy

//#8 - You want to teach sorting
Bubble sort